一、对象
   JavaScript的对象是一种无序集合数据类型，它由若干个键值对组成。
   JavaScript的对象用于描叙现实世界中的某个对象。
   JavaScript用一个{}表示一个对象。以XXX:XXX形式申明。
   访问属性是通过"."操作符完成的，但这要求属性名必须是一个
   有效的变量。
   对象属性访问方式另一种 对象名["属性名"]
   由于JavaScript的对象是动态类型，你可以自由的给一个对象
   1、添加属性或删除属性。
   ea:对象名.新属性名=值；
   删去属性的方法：delete 对象名.属性名(不推荐使
   2、检测对象的属性可以用in操作符
     "属性名" in 对象名
     //不能判断自己属性还是继承过来的属性
   3、hasOwnProperty()
   可以判断是是否是自己的属性
二、JavaScript把null/undefined/0/NaN/""都视为false
其他的值都是true;
三、循环
   for...in... 可以把一个对象的所有属性依次打印出来。
四、Map和Set
    Map是一组键值对的结构，具有极快的查找速度。
    初始化Map需要一个二维数组，或者直接初始化一个空Map。
    set()添加元素到Map
    get()得到元素的值、
    delete函数删除元素。
    has()检查是否有该元素。
    Set是一组key的集合，但不存储value，key不可以重复。
    add(key)方法可以添加元素到Set中。
    delete(key)方法可以删除元素。